{
  "name": "@fettle/action",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --format=cjs"
  },
  "dependencies": {
    "@fettle/core": "workspace:*"
  }
}
