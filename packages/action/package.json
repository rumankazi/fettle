{
  "name": "@fettle/action",
  "version": "0.1.0",
  "description": "GitHub Action wrapper around the maintenance-health grading engine.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node build.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@fettle/core": "workspace:*"
  }
}
