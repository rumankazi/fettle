node_modules
.pnpm-store
coverage
.vitest
.DS_Store

# Build output is generated, with one exception: the Action ships a bundled
# dist/index.js committed to the repo so consumers never run an install.
dist
!packages/action/dist
!packages/action/dist/index.js

*.tsbuildinfo

# Credentials. A GitHub App private key sitting in the working tree is one
# `git add -A` away from being public, and this repository is public.
*.pem
*.p12
*.key
.env
.env.*
