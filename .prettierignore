# Generated output. The Action bundle is committed but esbuild owns its
# formatting, not Prettier.
dist
pnpm-lock.yaml

# release-please rewrites this on every release and owns its layout. Formatting it
# ourselves would just lose the argument again next time.
CHANGELOG.md
